{
  "name": "fermion",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "typecheck": "./node_modules/.bin/tsc -p apps/api/tsconfig.json --noEmit && cd apps/web && ./node_modules/.bin/next build && cd ../.. && ./node_modules/.bin/tsc -p packages/ui/tsconfig.json --noEmit",
    "check-types": "pnpm typecheck"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.5",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=22"
  }
}
